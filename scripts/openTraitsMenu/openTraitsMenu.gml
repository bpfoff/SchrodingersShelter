if(object_get_parent(global.selected.object_index) == obj_person){
	
	room_goto(menu_personTraitsMenu);	
	
}

if(object_get_parent(global.selected.object_index) == obj_cat){
	
	room_goto(menu_catsTraitsMenu);	
	
}